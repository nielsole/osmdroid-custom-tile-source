# Very simple Android Map app

The only goal of this app is to have a fast app (60fps) with a custom tile layer.

The tile source can be changed in `src/main/java/com/niels_ole/customtileserver/MainActivity.kt`.
